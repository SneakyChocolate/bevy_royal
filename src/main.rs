
fn main() {
    println!("first run the server using 'cargo run -r --bin server' then run the a client using 'cargo run -r --bin client' and optional parameter the address:port which is default '127.0.0.1:7878'")
}

